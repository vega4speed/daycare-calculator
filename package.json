{
  "name": "daycare-calculator",
  "version": "0.1.0",
  "private": true,
  "description": "Month-by-month financial model for a childcare program launch. Static, zero-dependency, no build step.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
